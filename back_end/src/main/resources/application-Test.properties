# The Test profile must not validate Keycloak tokens, so the resource server
# auto-configuration stays disabled even if KEYCLOAK_ISSUER_URI is set in .env
spring.security.oauth2.resourceserver.jwt.issuer-uri=
